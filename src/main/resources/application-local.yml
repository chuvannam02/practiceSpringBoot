# DataSource
spring:
    datasource:
        url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:micro_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
        username: ${MYSQL_USER:root}
        password: ${MYSQL_PASSWORD:Huykenzj994}
        driver-class-name: com.mysql.cj.jdbc.Driver
        initialization-mode: always
    cache:
        type: redis
# Redis configuration
    data:
        redis:
            host: localhost
            port: 6379
            timeout: 60000 # 60s connection timeout
            time-to-live: ${REDIS_TIME_TO_LIVE:300000}