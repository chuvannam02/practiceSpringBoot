spring.application.name=practiceProject

# DataSource
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:db}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:micro_db}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:Huykenzj994}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=true

# Misc
spring.main.banner-mode=off
spring.main.allow-bean-definition-overriding=true

# Optional logging
# logging.level.root=warn
# spring.jpa.show-sql=true
# logging.level.org.hibernate=ERROR

# Example external API endpoint
product.url=https://dummyjson.com
