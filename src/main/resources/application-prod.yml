# DataSource
spring:
    datasource:
        url: jdbc:mysql://${MYSQL_HOST:host.docker.internal}:${MYSQL_PORT:3308}/${MYSQL_DATABASE:micro_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
        username: ${MYSQL_USER:root}
        password: ${MYSQL_PASSWORD:7qO3eq6a41}
        driver-class-name: com.mysql.cj.jdbc.Driver
        initialization-mode: always
    # Redis configuration
    data:
        redis:
            host: localhost
            port: 6379
            password: ${REDIS_PASSWORD:Z2CCpvzl3P}
            timeout: 60000 # 60s connection timeout
            time-to-live: ${REDIS_TIME_TO_LIVE:300000}
